/** @type {import('next').NextConfig} */
const nextConfig = {  
    images: {  
      domains: ['images.pexels.com', 'img.freepik.com'], // replace with your external image domain  
    },  
  }  

export default nextConfig;
